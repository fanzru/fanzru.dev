import React from "react";

const Logo = () => {
  return (
    <div className="flex items-baseline">
      <span className="font-[var(--font-playfair)] italic text-xl font-light tracking-wide">
        fanzru
      </span>
      <span className="font-[var(--font-geist-sans)] text-xl font-bold">
        .dev
      </span>
    </div>
  );
};

export default Logo;
